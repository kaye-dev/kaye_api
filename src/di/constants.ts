// controllers
export const dic = {
  UserController: 'UserController',
};

// models
export const dim = {
  UserModel: 'UserModel',
};

// decorators
export const did = {
  UserDecorator: 'UserDecorator',
};

// serializers
export const dis = {
  UserSerializer: 'UserSerializer',
};
